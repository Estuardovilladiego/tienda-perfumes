# Pega estas variables en Vercel → Project → Settings → Environment Variables
# (Production). Copia los VALORES desde tu archivo .env local.
#
# IMPORTANTE en Vercel (no en local):
# - ADMIN_PASSWORD → contraseña fuerte (no admin123)
# - NEXT_PUBLIC_SITE_URL → https://TU-PROYECTO.vercel.app (tras el 1er deploy)

DATABASE_URL
LOCAL_DATABASE_URL          # opcional; no necesaria en Vercel

ADMIN_EMAIL
ADMIN_PASSWORD
ADMIN_SESSION_SECRET

NEXT_PUBLIC_SITE_URL

CLOUDINARY_CLOUD_NAME
CLOUDINARY_UPLOAD_PRESET
CLOUDINARY_API_KEY
CLOUDINARY_API_SECRET

SMTP_HOST                   # smtp.gmail.com
SMTP_PORT                   # 587
SMTP_USER
SMTP_PASS                   # Gmail: 16 caracteres SIN espacios (ej. idtrvbuybzdyfjbo)
SMTP_FROM
SMTP_REPLY_TO               # opcional; mismo que SMTP_USER si no lo defines

NEXT_PUBLIC_PAGO_NEQUI
NEXT_PUBLIC_PAGO_DAVIPLATA
NEXT_PUBLIC_PAGO_LLAVES_ALIAS
NEXT_PUBLIC_PAGO_LLAVES_CELULAR
NEXT_PUBLIC_PAGO_BANCOLOMBIA_CUENTA
NEXT_PUBLIC_PAGO_BANCOLOMBIA_TIPO
NEXT_PUBLIC_PAGO_FALABELLA_CUENTA
NEXT_PUBLIC_PAGO_TITULAR
